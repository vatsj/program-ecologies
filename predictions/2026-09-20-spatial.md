# Spatial-structure verdict — written 2026-09-20, before the run

Death-birth Moran on a 32×32 torus (N = 1024), von Neumann neighbourhood
(k = 4): a random site dies, its neighbours compete to fill it with
probability ∝ exp(w·payoff), payoff = mean over the neighbour's own 4
interactions; mutation per birth at ε with εN ∈ {0.1, 1}, mutants from μ
over the behavioural classes of L_6 with ROLE, PD, w = 0.3; burn-in 10⁴
generations (N deaths), 10⁵ sampled, 5 seeds.  Control: the same rule on
the complete graph (well-mixed, N = 1024) at εN = 1.

**Verdicts (lattice, εN = 1):** P(C,C) > 0.3; `THEM(^C)` share > 0.3 and
greater than the ALLC share; `THEM(^D)` present at > 0.005.
**Control (well-mixed, εN = 1):** P(C,C) < 0.02.
**Falsifier:** lattice P(C,C) < 0.1.
